---
# Copy this file to a new name to create a post, e.g.
#   blog/content/2026-07-20-aria-nerja.md
# Files whose name starts with "_" are ignored by the build.
# After editing, run:  npm run build:blog

title: "Aria — Nerja cliffs"          # shown as the headline
slug: aria-nerja                        # the URL: /blog/aria-nerja/  (optional; defaults to filename without date)
date: 2026-07-20                        # YYYY-MM-DD, used for ordering
lang: en                                # "en" or "es" — sets the page language and CTA wording
location: "Nerja, Málaga"               # small eyebrow line
summary: "One line that appears under the title and as the social/search description."
model: "Aria"                           # model alias (first name / stage name only unless she consents to more)
model_link: ""                          # optional: her Instagram/portfolio (only with her consent)

# Cover image = the social-share / index thumbnail. Bare filename → served from
# R2 at /gallery/<name>. You can also use a full https:// URL or a /path.
cover: "Z52_0501.jpg"

# Carousel photos, in order. Same rules as cover.
images:
  - "Z52_0501.jpg"
  - "Z52_0537-small.jpg"
  - "Z52_1162-small.jpg"

# Optional pull-quote from the model (her words — highest-trust element).
quote: ""
quote_by: ""

# Optional extra credits (any key: value pairs).
credits:
  # MUA: "Name"
  # Styling: "Name"

published: true                         # set false to keep a draft out of the build
---

Write the story here in plain Markdown — how the shoot came together, the concept,
the location, what made it memorable. Two or three short paragraphs is plenty.

Keep it human and narrative, not gear specs. This is what lets a future model
"meet" you before she ever writes.
